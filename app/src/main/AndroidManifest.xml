<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- API 24-32: necessário para ler arquivos fora do diretório privado -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- API 33+: permissão granular para imagens (PDFs via SAF não precisam de permissão extra) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConveniarEventos"> <activity
        android:name=".ui.DetalhesEvento"
        android:exported="false" />

        <activity
            android:name=".ui.CadastroUsuario"
            android:exported="false" />

        <activity android:name=".ui.AreaInscrito" android:exported="false" />

        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/Theme.ConveniarEventos.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.InscricaoEvento"
            android:exported="false"/>
        <activity
            android:name=".ui.TermosAceiteEvento"
            android:exported="false"
            android:parentActivityName=".ui.DetalhesEvento" />
        <activity
            android:name=".ui.MenuSelecione"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>